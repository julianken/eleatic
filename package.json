{
  "name": "eleatic-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "The eleatic family — a local-first eval / LLM-observability toolkit. Packages live under packages/.",
  "license": "MIT",
  "author": "Julian Kennon",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "example": "npm run example -w @eleatic/eval"
  }
}
